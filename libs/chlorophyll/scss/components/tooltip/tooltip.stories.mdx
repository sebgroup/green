import { Meta, Canvas } from '@storybook/addon-docs';

export const Template = (args) => {
  return `
        <div style="display:grid; place-items: center; height: 30rem;">
          <div>
            <button class="ghost">
              <div inert="true" role="tooltip" id="example-tool-tip" tip-position="${args.tipPosition}">${args.tipContent}</div>
              Hover me
            </button>
          </div>
        </div>
  `
}

<Meta
  title="Components/Tooltip"
  argTypes={{
    tipPosition: {
      control: 'select',
      options: ["top", "bottom", "right", "left"]
    },
  }}
/>

<Story
  name="Tooltip"
   args={{
    tipPosition: "top",
    tipContent: "The text will appear on your account. If you leave the field blank, the first 12 digits of the recipient account will be displayed instead."
   }}
>
    {Template.bind({})}
</Story>

# Tooltip

This is the documentation page for tooltip

<Canvas>
  <button>
    <div inert role="tooltip" id="example-tool-tip" tip-position="top">This is a tooltip</div>
    Top
  </button>
  <button>
    <div inert role="tooltip" id="example-tool-tip" tip-position="bottom">This is a tooltip</div>
    Bottom
  </button>
</Canvas>

## Long example

<Canvas>
    <button>
      <div inert role="tooltip" id="example-tool-tip" tip-position="bottom">The text will appear on your account. If you leave the field blank, the first 12 digits of the recipient account will be displayed instead.</div>
      Top
    </button>
</Canvas>
